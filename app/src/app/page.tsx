import ConfigureTransaction from "@/components/configure-transaction";

export default function Home() {
  return (
    <main className="flex flex-col items-center justify-normal gap-6">
      <ConfigureTransaction />
    </main>
  );
}
